set previewer ~/.config/lf/preview
set preview true
set hidden true
set drawbox true
set icons true
set ignorecase true
set scrolloff 10
set shellopts '-eu'
set ifs "\n"

### REMOVE UNUSED DEFAULT BINDS ###
map m
map n
map o
map d
map c
map e
map f
map '"'
map "'"

### CORE NAVIGATION ###
map <enter> open
map x cut
map y copy
map R reload
map C clear
map U unselect
map H top
map L bottom
map . set hidden!

### COMMON JUMP SHORTCUTS ###
map gh :cd ~
map gc :cd ~/.config
map gd :cd ~/Downloads
map gD :cd ~/Documents
map gP :cd ~/Projects
map gs :cd ~/Scripts
map gp :cd ~/Pictures
map gm :cd ~/Music
map gv :cd ~/Videos
map gG :cd ~/Github

map ee $$f

### Custom Commands ###

# Go to media dir
cmd media ${{
  lf -remote "send $id cd '/run/media/$USER/'"
}}
map gM media

### FILE CREATION ###
cmd mk ${{
  printf "Name: "
  read -r name
  [ -z "$name" ] && exit

  printf "Type (dir/file/script/proj): "
  read -r type
  case "$type" in
    dir|d) mkdir -pv -- "$name" ;;
    file|f) :> "$name" ;;
    script|scr|s) mkscript -n "$name" && lf -remote "send $id cd '~/Scripts/'" ;;
    project|proj|p)
      projtool mkproj -n "$name" || exit
      esc_name=$(printf '%s' "$name" | sed 's/\\/\\\\/g; s/"/\\"/g')
      lf -remote "send $id cd \"$HOME/Projects/$esc_name\""
      ;;
    *) echo "Unknown type." ;;
  esac
  lf -remote 'send reload'
}}
map mm mk

### DELETE / TRASH ###
cmd trash %{{
    sep=$(printf '\037')
    for f in $fx; do
        printf 'trash%s%s\n' "$sep" "$f" >> /tmp/lf_undo_$USER
        trash-put -vr "$f"
    done
}}
cmd trash-list ${{
    trash-list | moar
}}
cmd trash-restore ${{
    trash-restore
}}
cmd trash-empty %{{
    trash-empty
}}
cmd remove %{{
    for f in $fx; do
        rm -rfv "$f"
    done
}}
map DD remove
map dd trash
map tt trash-list
map tr trash-restore
map te trash-empty

### PASTE (with undo support) ###
cmd paste %{{
    sep=$(printf '\037')
    sel_file="${XDG_DATA_HOME:-$HOME/.local/share}/lf/files"
    [ -f "$sel_file" ] || exit
    load=$(cat "$sel_file")
    mode=$(printf '%s\n' "$load" | sed -n '1p')
    list=$(printf '%s\n' "$load" | sed '1d')
    [ -z "$list" ] && exit

    printf '%s\n' "$list" | while IFS= read -r src; do
        [ -z "$src" ] && continue
        base=$(basename -- "$src")
        target="$PWD/$base"
        n=1
        while [ -e "$target" ]; do
            target="$PWD/${base}_$n"
            n=$((n + 1))
        done
        if [ "$mode" = 'copy' ]; then
            cp -r -- "$src" "$target"
        else
            mv -- "$src" "$target"
        fi
        printf 'paste%s%s\n' "$sep" "$target" >> /tmp/lf_undo_$USER
    done
    lf -remote 'send clear'
    lf -remote 'send reload'
}}
map p paste

### TOGGLE EXECUTABLE / PERMISSIONS ###
cmd chmod %{{
  sep=$(printf '\037')
  oldperms=$(stat -c "%a" "$f")
  printf "Chmod bits: "
  read -r bits
  [ -z "$bits" ] && exit
  chmod "$bits" "$f"
  printf 'chmod%s%s%s%s\n' "$sep" "$oldperms" "$sep" "$f" >> /tmp/lf_undo_$USER
  lf -remote 'send reload'
}}
map et chmod

### ARCHIVE / EXTRACT ###
cmd extract %{{
  for f in $fx; do
    archives extract "$f"
  done
  lf -remote 'send reload'
}}
map ex extract

cmd archive %{{
  printf " Archive name (.tar.gz/.zip/.rar): "
  read -r out
  [ -z "$out" ] && exit
  archives archive "$out" $fx
  lf -remote 'send reload'
}}
map er archive

### FZF JUMP ###
cmd fzf_jump ${{
  res=$(fd --hidden --exclude .git . 2>/dev/null | \
    fzf --reverse \
        --preview '[ -d {} ] && eza -a --tree --level=2 --group-directories-first --color=always --icons {} 2>/dev/null || batcat --color=always --style=numbers --line-range=:100 {} 2>/dev/null || cat {}' \
        </dev/tty)
  [ -z "$res" ] && exit
  esc_res=$(printf '%s' "$res" | sed 's/\\/\\\\/g; s/"/\\"/g')
  if [ -d "$res" ]; then
    lf -remote "send $id cd \"$esc_res\""
  else
    esc_dir=$(dirname "$res" | sed 's/\\/\\\\/g; s/"/\\"/g')
    lf -remote "send $id cd \"$esc_dir\""
    lf -remote "send $id select \"$esc_res\""
    lf -remote "send $id open"
  fi
}}
map fj fzf_jump

### FOLLOW SYMLINK ###
cmd follow-link ${{
  [ ! -L "$f" ] && echo "Not a symlink." && exit
  target=$(readlink -f "$f")
  esc_target=$(printf '%s' "$target" | sed 's/\\/\\\\/g; s/"/\\"/g')
  if [ -d "$target" ]; then
    lf -remote "send $id cd \"$esc_target\""
  elif [ -f "$target" ]; then
    esc_dir=$(dirname "$target" | sed 's/\\/\\\\/g; s/"/\\"/g')
    lf -remote "send $id cd \"$esc_dir\""
    ${EDITOR:-nvim} "$target"
  fi
}}
map gf follow-link

### RENAME ###
cmd rename ${{
  sep=$(printf '\037')
  old="$f"
  oldname=$(basename "$old")
  printf "New name: "
  read -r newname
  [ -z "$newname" ] && exit
  newpath="$(dirname "$old")/$newname"
  mv -- "$old" "$newpath"
  printf 'rename%s%s%s%s\n' "$sep" "$old" "$sep" "$newpath" >> /tmp/lf_undo_$USER
  lf -remote 'send reload'
}}
map r rename

### OPEN FILE ###
cmd open ${{
  mime=$(file --mime-type -Lb "$f")
  ext="${f##*.}"

  case "$mime" in
    text/*|application/json|application/xml)
      ${EDITOR:-nvim} "$f" ;;
    image/*)
      sxiv "$f" 2>/dev/null || imv "$f" 2>/dev/null || chafa "$f" ;;
    video/*|audio/*)
      mpv "$f" ;;
    application/pdf)
      zathura "$f" 2>/dev/null || pdftotext "$f" - | less ;;
    *)
      # fallback based on extension
      case "$ext" in
        sh|py|c|cpp|lua|rs|go|js|ts|md|json|yml|yaml)
          ${EDITOR:-nvim} "$f" ;;
        jpg|jpeg|png|gif|bmp|webp|svg)
          sxiv "$f" 2>/dev/null || imv "$f" ;;
        mp4|mkv|mp3|flac|ogg|wav)
          mpv "$f" ;;
        pdf)
          zathura "$f" 2>/dev/null || pdftotext "$f" - | less ;;
        *) xdg-open "$f" >/dev/null 2>&1 ;;
      esac
      ;;
  esac
}}
map <enter> open

# Iso Burner
cmd burn ${{
    [ -z "$f" ] && echo "No file selected." && exit
    echo "󰈸 Burning ISO: $f"
    caligula burn "$f"
}}
map bi burn

### WALLPAPER ###
cmd setbg ${{
  xwallpaper --stretch "$f" 2>/dev/null || echo "Failed to set wallpaper"
}}
map bg setbg

### DRAG (DRAGON) ###
cmd drag ${{
  dragon-drop -a -x -T $fx
}}
map dr drag

### Secure Copy ###
cmd scp ${{
  read -rp "Port (default 22): " port
  port="${port:-22}"
  read -rp "Host (user@host:/path): " host
  [ -z "$host" ] && exit
  if [ -n "$fx" ]; then
    printf '%s\n' "$fx" | xargs -d '\n' -I{} scp -P "$port" "{}" "$host"
  else
    scp -P "$port" "$f" "$host"
  fi
}}
map cs scp

cmd undo ${{
  sep=$(printf '\037')
  [ ! -f /tmp/lf_undo_$USER ] && echo "Nothing to undo." && exit
  last=$(tail -1 /tmp/lf_undo_$USER)
  action=$(printf '%s' "$last" | cut -d"$sep" -f1)
  data=$(printf '%s' "$last" | cut -d"$sep" -f2-)

  case "$action" in
    paste)
      rm -rf "$data"
      sed -i '$d' /tmp/lf_undo_$USER
      lf -remote 'send reload'
      ;;
    rename)
      old=$(printf '%s' "$data" | cut -d"$sep" -f1)
      new=$(printf '%s' "$data" | cut -d"$sep" -f2)
      mv -- "$new" "$old"
      sed -i '$d' /tmp/lf_undo_$USER
      lf -remote 'send reload'
      ;;
    chmod)
      perms=$(printf '%s' "$data" | cut -d"$sep" -f1)
      fp=$(printf '%s' "$data" | cut -d"$sep" -f2)
      chmod "$perms" "$fp"
      sed -i '$d' /tmp/lf_undo_$USER
      lf -remote 'send reload'
      ;;
    trash)
      path="$data"
      filename=$(basename "$path")
      trashinfo=~/.local/share/Trash/info/${filename}.trashinfo
      trashed=~/.local/share/Trash/files/${filename}
      if [ -e "$trashed" ] && [ -f "$trashinfo" ]; then
        mv "$trashed" "$path"
        rm "$trashinfo"
        sed -i '$d' /tmp/lf_undo_$USER
      else
        echo "Can't find $filename in trash"
      fi
      lf -remote 'send reload'
      ;;
  esac
}}
map u undo

# For Vim Users
# vim:ft=conf
