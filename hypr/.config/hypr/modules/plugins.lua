-- hyprsplit is now a lua library, no plugin loading needed
-- Clone the repo: git clone https://github.com/shezdy/hyprsplit ~/.config/hypr/hyprsplit
hs = require("hyprsplit")
hs.config({ num_workspaces = 5 })
